import 'package:flutter/material.dart';

class temperaturePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Center(child: Text('Ultrasonic...')),
      ),
    );
  }
}
